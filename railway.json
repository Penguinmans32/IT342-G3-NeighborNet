{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacks": {
      "providers": ["java"]
    },
    "buildCommand": "cd backend && ./mvnw clean package -DskipTests || cd backend && apt-get update && apt-get install -y maven && cd backend && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "cd backend && java -jar target/NeighborNet-Backend-0.0.1-SNAPSHOT.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "variables": {
    "NIXPACKS_JDK_VERSION": "21"
  }
}